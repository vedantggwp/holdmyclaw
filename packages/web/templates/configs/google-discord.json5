// OpenClaw config: Google Gemini + Discord
// Generated by HoldMyClaw — verified against official OpenClaw schema
{
  gateway: {
    port: 18789,
    bind: "lan",
    auth: {
      token: "${OPENCLAW_GATEWAY_TOKEN}"
    }
  },

  env: {
    GOOGLE_GENERATIVE_AI_API_KEY: "${LLM_API_KEY}"
  },

  agents: {
    defaults: {
      model: {
        primary: "google/gemini-2.5-pro",
        fallbacks: ["google/gemini-2.5-flash"]
      },
      contextTokens: 128000,
      timeoutSeconds: 600
    }
  },

  channels: {
    discord: {
      enabled: true,
      token: "${CHANNEL_TOKEN}",
      dm: {
        enabled: true,
        policy: "${DM_POLICY}"
      },
      guilds: {},
      historyLimit: 20,
      textChunkLimit: 2000
    }
  },

  session: {
    reset: {
      mode: "daily",
      atHour: 4
    }
  },

  skills: {
    allowBundled: ["clawhub"]
  }
}
