// OpenClaw config: OpenAI + Discord
// Generated by HoldMyClaw — verified against official OpenClaw schema
{
  gateway: {
    port: 18789,
    bind: "lan",
    auth: {
      token: "${OPENCLAW_GATEWAY_TOKEN}"
    }
  },

  env: {
    OPENAI_API_KEY: "${LLM_API_KEY}"
  },

  agents: {
    defaults: {
      model: {
        primary: "openai/gpt-4.1",
        fallbacks: ["openai/gpt-4.1-mini"]
      },
      contextTokens: 128000,
      timeoutSeconds: 600
    }
  },

  channels: {
    discord: {
      enabled: true,
      token: "${CHANNEL_TOKEN}",
      dm: {
        enabled: true,
        policy: "${DM_POLICY}"
      },
      guilds: {},
      historyLimit: 20,
      textChunkLimit: 2000
    }
  },

  session: {
    reset: {
      mode: "daily",
      atHour: 4
    }
  },

  skills: {
    allowBundled: ["clawhub"]
  }
}
