// OpenClaw config: Google Gemini + Telegram
// Generated by HoldMyClaw — verified against official OpenClaw schema
{
  gateway: {
    port: 18789,
    bind: "lan",
    auth: {
      token: "${OPENCLAW_GATEWAY_TOKEN}"
    }
  },

  env: {
    GOOGLE_GENERATIVE_AI_API_KEY: "${LLM_API_KEY}"
  },

  agents: {
    defaults: {
      model: {
        primary: "google/gemini-2.5-pro",
        fallbacks: ["google/gemini-2.5-flash"]
      },
      contextTokens: 128000,
      timeoutSeconds: 600
    }
  },

  channels: {
    telegram: {
      enabled: true,
      botToken: "${CHANNEL_TOKEN}",
      dmPolicy: "${DM_POLICY}",
      allowFrom: [${ALLOW_FROM}],
      groups: {
        "*": { requireMention: true }
      },
      streamMode: "partial",
      reactionNotifications: "own",
      reactionLevel: "minimal"
    }
  },

  session: {
    reset: {
      mode: "daily",
      atHour: 4
    }
  },

  skills: {
    allowBundled: ["clawhub"]
  }
}
